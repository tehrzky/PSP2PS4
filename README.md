# PSP 2 PS4 AIO — GUI

Builds a PS4-installable `.pkg` from a PSP `.iso`, or extracts/builds custom emulator PKGs.

## Download

Grab `PSP2PS4_GUI.exe` from the [Releases](../../releases) page.

## Setup

Place the EXE next to a `tools/` folder containing the PSP2PS4 toolchain:
